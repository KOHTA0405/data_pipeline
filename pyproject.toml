[project]
name = "data-pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dagster>=1.11.9",
    "dagster-dbt>=0.27.9",
    "dbt-bigquery>=1.10.2",
    "dbt-core>=1.10.11",
    "ruff>=0.13.0",
]

[dependency-groups]
dev = [
    "jupyterlab>=4.4.7",
]
