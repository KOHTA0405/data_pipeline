select
    invocation_id,
    run_started_at,
    run_completed_at,
    generated_at,
    command,
    dbt_version,
    elementary_version,
    full_refresh,
    invocation_vars,
    vars,
    target_name,
    target_database,
    target_schema,
    target_profile_name,
    threads,
    selected,
    yaml_selector,
    job_id,
    job_name,
    job_run_id,
    env,
    env_id,
    project_id,
    cause_category,
    cause,
    pull_request_id,
    git_sha,
    orchestrator,
    job_url,
    account_id
from {{ source('elementary_run_results', 'dbt_invocations') }}
