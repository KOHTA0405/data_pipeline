select
    model_execution_id,
    unique_id,
    invocation_id,
    query_id,
    name,
    generated_at,
    status,
    full_refresh,
    message,
    execution_time,
    execute_started_at,
    execute_completed_at,
    compile_started_at,
    compile_completed_at,
    compiled_code,
    adapter_response,
    thread_id,
    group_name,
    database_name,
    schema_name,
    materialization,
    tags,
    package_name,
    path,
    original_path,
    owner,
    alias
from {{ source('elementary_run_results', 'snapshot_run_results') }}
