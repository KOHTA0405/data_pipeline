# on:
#   pull_request:
#     branches:
#       - main

# 内容:

# Terraformの初期設定（プロバイダ認証など）。

# terraform fmt --check

# terraform validate

# tflint / checkov などの静的解析ツール

# terraform plan (ターゲットはprd環境。出力結果をPRコメントとして投稿)